name: PCHM30 A16 Run26 ReSukiSU SUSFS 2.3.0
# Registered on main so Actions can see it. The real build lives on pchm30-a16-run26-susfs230-split.

on:
  workflow_dispatch:
  push:
    branches: [pchm30-a16-run26-susfs230-split]
    paths:
      - '.github/workflows/pchm30-a16-run26-resukisu-susfs230.yml'
      - '.github/scripts/run25-susfs230-adapt.sh'
      - '.github/scripts/run26-disable-kpm.sh'

jobs:
  noop:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-22.04
    steps:
      - name: Workflow registered
        run: echo 'Run26 ReSukiSU workflow is registered on main; real build lives on pchm30-a16-run26-susfs230-split'
